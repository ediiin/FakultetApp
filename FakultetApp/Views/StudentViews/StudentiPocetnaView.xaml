<UserControl x:Class="FakultetApp.Views.StudentViews.StudentiPocetnaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="10">

        <!-- MASTER VIEW (Lista objava) -->
        <Grid x:Name="PrikazListe" Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Welcome Poruka -->
                <RowDefinition Height="*"/>
                <!-- Glavni dio (75/25) -->
            </Grid.RowDefinitions>

            <!-- ZAGLAVLJE -->
            <Border Grid.Row="0" Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="20" Margin="0,0,0,20" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <StackPanel>
                    <TextBlock x:Name="lblDobrodosao" Text="Dobrodošli natrag!" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                    <TextBlock Text="Ovdje možete pregledati najnovije obavijesti sa vaših predmeta i globalna obavještenja fakulteta." FontSize="13" Foreground="{DynamicResource PrimaryText}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- GLAVNI DIO (75% Obavijesti / 25% Widgeti) -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="75*"/>
                    <ColumnDefinition Width="25*"/>
                </Grid.ColumnDefinitions>

                <!-- LIJEVA STRANA: FEED OBAVIJESTI I PAGINACIJA -->
                <Grid Grid.Column="0" Margin="0,0,20,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <!-- Scrollable Feed -->
                        <RowDefinition Height="Auto"/>
                        <!-- Paginacija -->
                    </Grid.RowDefinitions>

                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="icObavijesti">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,15" Padding="18">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="{DynamicResource AccentColor}" CornerRadius="4" Padding="8,3" Margin="0,0,10,0">
                                                    <TextBlock Text="{Binding OznakaPredmeta}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                                </Border>
                                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                                    <TextBlock Text="✍️ " FontSize="12" Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding AutorIme}" Foreground="{DynamicResource PrimaryText}" FontSize="12" FontWeight="Medium" Margin="0,0,15,0"/>
                                                    <TextBlock Text="📅 " FontSize="12" Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding DatumObjave, StringFormat={}{0:dd.MM.yyyy. HH:mm}}" Foreground="{DynamicResource PrimaryText}" FontSize="12"/>
                                                </StackPanel>
                                            </Grid>
                                            <TextBlock Text="{Binding Naslov}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,8" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding KratkiSadrzaj}" FontSize="13" Foreground="{DynamicResource PrimaryText}" Opacity="0.9" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,10"/>

                                            <!-- DUGME ZA DETALJE -->
                                            <Button Content="Pročitaj više" Tag="{Binding}" Click="BtnProcitajVise_Click" HorizontalAlignment="Left" Background="Transparent" Foreground="{DynamicResource ButtonColor}" BorderThickness="0" Cursor="Hand" FontWeight="Bold" Padding="0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- PAGINACIJA DUGMAD -->
                    <StackPanel x:Name="PanelPaginacija" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </Grid>

                <!-- DESNA STRANA: WIDGETI ZA DOKUMENTE I USPJEH -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Dokumenti ostaju na VRHU -->
                        <RowDefinition Height="*"/>
                        <!-- Prosjek ide na DNO -->
                    </Grid.RowDefinitions>

                    <!-- ZADNJI DOKUMENTI (VRH) -->
                    <Border Grid.Row="0" Background="{DynamicResource CardBackground}" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="Zadnji dokumenti" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,15"/>

                            <ItemsControl x:Name="icDokumenti">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,12" Background="Transparent" Cursor="Hand" MouseLeftButtonUp="Dokument_MouseLeftButtonUp">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <!-- Ikonica -->
                                            <Border Grid.Column="0" Background="{DynamicResource AccentColor}" CornerRadius="4" Padding="6" Margin="0,0,10,0" VerticalAlignment="Center">
                                                <TextBlock Text="📄" FontSize="14"/>
                                            </Border>
                                            <!-- Naziv i Datum -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <!-- VRAĆENO NA Naziv (i Datum) -->
                                                <TextBlock Text="{Binding Naziv}" Foreground="{DynamicResource PrimaryText}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding Naziv}"/>
                                                <TextBlock Text="{Binding Datum, StringFormat={}{0:dd.MM.yyyy.}}" Foreground="{DynamicResource PrimaryText}" FontSize="11" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- USPJEH STUDENTA (ZAKUCAN ZA DNO SA SCROLL-OM) -->
                    <Border Grid.Row="1" x:Name="karticaUspjeh" VerticalAlignment="Bottom" Background="{DynamicResource CardBackground}" CornerRadius="8" BorderThickness="1" BorderBrush="{DynamicResource BorderColor}" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Vaš uspjeh" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,15"/>

                            <!-- SCROLLVIEWER SA MAX VISINOM ZA SVE GODINE -->
                            <ScrollViewer MaxHeight="180" VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                                <ItemsControl ItemsSource="{Binding UspjehPoGodinama}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,8">
                                                <TextBlock Text="{Binding GodinaOpis}" Foreground="{DynamicResource PrimaryText}" FontSize="13" VerticalAlignment="Center"/>
                                                <Border Background="{DynamicResource WindowBackground}" CornerRadius="4" Padding="8,2" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding ProsjekGodine, StringFormat={}{0:F2}}" Foreground="{DynamicResource PrimaryText}" FontWeight="Bold" FontSize="13"/>
                                                </Border>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>

                            <Separator Margin="0,10,0,15" Background="{DynamicResource BorderColor}"/>

                            <Grid>
                                <TextBlock Text="Ukupni prosjek:" Foreground="{DynamicResource PrimaryText}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding UkupniProsjek, StringFormat={}{0:F2}}" Foreground="{DynamicResource PrimaryText}" FontWeight="Bold" FontSize="18" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>

        <!-- DETAIL VIEW (Detaljan prikaz obavijesti) -->
        <Grid x:Name="PrikazDetalja" Visibility="Collapsed" Background="{DynamicResource WindowBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="⬅️ Nazad na objave" Click="BtnNazad_Click" HorizontalAlignment="Left" Background="Transparent" Foreground="{DynamicResource PrimaryText}" BorderThickness="0" Cursor="Hand" FontWeight="Bold" Margin="0,0,0,20" FontSize="14"/>

            <Border Grid.Row="1" Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="30" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Badge i Autor -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Border Background="{DynamicResource AccentColor}" CornerRadius="4" Padding="8,3" Margin="0,0,15,0">
                                <TextBlock x:Name="txtDetaljiOznaka" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                            </Border>
                            <TextBlock Text="✍️ Autor: " Foreground="{DynamicResource PrimaryText}" FontSize="13" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtDetaljiAutor" Foreground="{DynamicResource PrimaryText}" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
                            <TextBlock Text="📅 Datum: " Foreground="{DynamicResource PrimaryText}" FontSize="13" VerticalAlignment="Center"/>
                            <TextBlock x:Name="txtDetaljiDatum" Foreground="{DynamicResource PrimaryText}" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- Naslov i Sadržaj -->
                        <TextBlock x:Name="txtDetaljiNaslov" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,20" TextWrapping="Wrap"/>
                        <TextBlock x:Name="txtDetaljiSadrzaj" FontSize="15" Foreground="{DynamicResource PrimaryText}" LineHeight="24" TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>