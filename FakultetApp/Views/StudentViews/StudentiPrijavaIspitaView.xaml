<UserControl x:Class="FakultetApp.Views.StudentViews.StudentiPrijavaIspitaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="{DynamicResource WindowBackground}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Naslov -->
        <TextBlock Grid.Row="0" Text="Prijava ispita" FontSize="26" FontWeight="Bold" 
                   Foreground="{DynamicResource Naslov}" Margin="0,0,0,20"/>

        <!-- Tabela dostupnih ispita -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackground}" CornerRadius="10" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.05" Color="Black"/>
            </Border.Effect>

            <DataGrid x:Name="dgDostupniIspiti" AutoGenerateColumns="False" IsReadOnly="True" 
                      SelectionMode="Single" Background="Transparent" BorderThickness="0"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowBackground="{DynamicResource CardBackground}"
                      AlternatingRowBackground="{DynamicResource WindowBackground}"
                      Foreground="{DynamicResource PrimaryText}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Predmet" Binding="{Binding Predmet.Naziv}" Width="*"/>
                    <DataGridTextColumn Header="Datum održavanja" Binding="{Binding DatumOdrzavanja, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Width="150"/>
                    <DataGridTextColumn Header="Max Bodova" Binding="{Binding MaxBrojBodova}" Width="100"/>
                    <DataGridTextColumn Header="Zadaci" Binding="{Binding BrojZadataka}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Akcione kontrole -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock x:Name="lblGreska" Text="Molimo odaberite ispit iz tabele." 
                       Foreground="{DynamicResource Error}" VerticalAlignment="Center" Margin="0,0,15,0" Visibility="Hidden"/>

            <Button Content="Prijavi ispit" Width="150" Height="45" Background="{DynamicResource ButtonColor}" 
                    Foreground="White" FontWeight="Bold" FontSize="16" Cursor="Hand" Click="BtnPrijaviIspit_Click" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>