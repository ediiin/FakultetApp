<UserControl x:Class="FakultetApp.Views.StudentViews.StudentiPrijavaIspitaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="{DynamicResource WindowBackground}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Naslov 1 -->
            <RowDefinition Height="*"/>
            <!-- Tabela dostupnih -->
            <RowDefinition Height="Auto"/>
            <!-- Naslov 2 -->
            <RowDefinition Height="*"/>
            <!-- Tabela prijavljenih -->
        </Grid.RowDefinitions>

        <!-- 1. SEKCIJA: DOSTUPNI ISPITI -->
        <TextBlock Grid.Row="0" Text="Dostupni ispiti za prijavu" FontSize="22" FontWeight="Bold" 
                   Foreground="{DynamicResource Naslov}" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="{DynamicResource CardBackground}" CornerRadius="10" Padding="10" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.05" Color="Black"/>
            </Border.Effect>

            <DataGrid x:Name="dgDostupniIspiti" AutoGenerateColumns="False" IsReadOnly="True" 
                      SelectionMode="Single" Background="Transparent" BorderThickness="0"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowBackground="{DynamicResource CardBackground}"
                      AlternatingRowBackground="{DynamicResource WindowBackground}"
                      Foreground="{DynamicResource PrimaryText}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Predmet" Binding="{Binding Predmet.Naziv}" Width="*"/>
                    <DataGridTextColumn Header="Datum održavanja" Binding="{Binding DatumOdrzavanja, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Width="150"/>
                    <DataGridTextColumn Header="Max Bodova" Binding="{Binding MaxBrojBodova}" Width="100"/>
                    <!-- Dugme u redu -->
                    <DataGridTemplateColumn Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Prijavi" Background="{DynamicResource ButtonColor}" Foreground="White" 
                                        FontWeight="Bold" Padding="10,5" Cursor="Hand" BorderThickness="0"
                                        Click="BtnPrijavi_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- 2. SEKCIJA: MOJE PRIJAVE -->
        <TextBlock Grid.Row="2" Text="Prijavljeni ispiti" FontSize="22" FontWeight="Bold" 
                   Foreground="{DynamicResource Naslov}" Margin="0,0,0,15"/>

        <Border Grid.Row="3" Background="{DynamicResource CardBackground}" CornerRadius="10" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.05" Color="Black"/>
            </Border.Effect>

            <DataGrid x:Name="dgPrijavljeniIspiti" AutoGenerateColumns="False" IsReadOnly="True" 
                      SelectionMode="Single" Background="Transparent" BorderThickness="0"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowBackground="{DynamicResource CardBackground}"
                      AlternatingRowBackground="{DynamicResource WindowBackground}"
                      Foreground="{DynamicResource PrimaryText}">
                <DataGrid.Columns>
                    <!-- Ovdje je Binding Ispit.Predmet.Naziv jer je izvorna klasa StudentIspit -->
                    <DataGridTextColumn Header="Predmet" Binding="{Binding Ispit.Predmet.Naziv}" Width="*"/>
                    <DataGridTextColumn Header="Datum održavanja" Binding="{Binding Ispit.DatumOdrzavanja, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Width="150"/>
                    <DataGridTextColumn Header="Cijena" Binding="{Binding Cijena, StringFormat=\{0:F2\} KM}" Width="100"/>
                    <!-- Dugme za odjavu u redu -->
                    <DataGridTemplateColumn Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Odjavi" Background="#E53E3E" Foreground="White" 
                                        FontWeight="Bold" Padding="10,5" Cursor="Hand" BorderThickness="0"
                                        Click="BtnOdjavi_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="4"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>