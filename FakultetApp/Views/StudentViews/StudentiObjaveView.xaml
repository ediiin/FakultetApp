<UserControl x:Class="FakultetApp.Views.StudentViews.StudentiObjaveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">

    <!-- Glavni kontejner je centriran i ima maksimalnu širinu -->
    <Grid Margin="10" HorizontalAlignment="Center" MaxWidth="850" Width="1000">

        <!-- MASTER VIEW (Filteri, Lista i Paginacija) -->
        <Grid x:Name="PrikazListe" Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Zaglavlje i Filteri (Row 0) -->
                <RowDefinition Height="*"/>
                <!-- Lista objava (Row 1) -->
                <RowDefinition Height="Auto"/>
                <!-- Paginacija (Row 2) -->
            </Grid.RowDefinitions>

            <!-- ZAGLAVLJE I FILTERI -->
            <Border Grid.Row="0" Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="20" Margin="0,0,0,20" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Objave po predmetima" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                    <TextBlock Text="Filtrirajte obavijesti odabirom godine i specifičnog predmeta." FontSize="13" Foreground="{DynamicResource PrimaryText}" Margin="0,5,0,15"/>

                    <!-- FILTERI (U jednom redu) -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="15"/>
                            <!-- Razmak -->
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Odabir Godine -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Godina studija:" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cmbGodina" SelectionChanged="CmbGodina_SelectionChanged" Height="35" FontSize="14"/>
                        </StackPanel>

                        <!-- Odabir Predmeta -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Predmet:" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cmbPredmet" SelectionChanged="CmbPredmet_SelectionChanged" Height="35" FontSize="14" IsEnabled="False"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- LISTA OBJAVA -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Prikazuje se ako nema objava -->
                    <TextBlock x:Name="txtNemaObjava" Text="Nema pronađenih objava za odabrani predmet." Foreground="{DynamicResource PrimaryText}" FontSize="15" HorizontalAlignment="Center" Margin="0,30,0,0" Visibility="Collapsed"/>

                    <ItemsControl x:Name="icObavijesti">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,15" Padding="18">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,10">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                <TextBlock Text="✍️ Autor: " Foreground="{DynamicResource PrimaryText}" FontSize="12"/>
                                                <TextBlock Text="{Binding AutorIme}" Foreground="{DynamicResource PrimaryText}" FontSize="12" FontWeight="Medium" Margin="0,0,15,0"/>
                                                <TextBlock Text="📅 " Foreground="{DynamicResource PrimaryText}" FontSize="12" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding DatumObjave, StringFormat={}{0:dd.MM.yyyy. HH:mm}}" Foreground="{DynamicResource PrimaryText}" FontSize="12"/>
                                            </StackPanel>
                                        </Grid>
                                        <TextBlock Text="{Binding Naslov}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,8" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding KratkiSadrzaj}" FontSize="14" Foreground="{DynamicResource PrimaryText}" Opacity="0.9" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,10"/>

                                        <!-- DUGME ZA DETALJE -->
                                        <Button Content="Pročitaj više" Tag="{Binding}" Click="BtnProcitajVise_Click" HorizontalAlignment="Left" Background="Transparent" Foreground="{DynamicResource ButtonColor}" BorderThickness="0" Cursor="Hand" FontWeight="Bold" Padding="0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <!-- OVDJE JE DODATA PAGINACIJA (Row 2) -->
            <StackPanel x:Name="PanelPaginacija" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,10"/>

        </Grid>

        <!-- DETAIL VIEW (Detaljan prikaz obavijesti) -->
        <Grid x:Name="PrikazDetalja" Visibility="Collapsed" Background="{DynamicResource WindowBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="⬅️ Nazad na listu" Click="BtnNazad_Click" HorizontalAlignment="Left" Background="Transparent" Foreground="{DynamicResource PrimaryText}" BorderThickness="0" Cursor="Hand" FontWeight="Bold" Margin="0,0,0,20" FontSize="14"/>

            <Border Grid.Row="1" Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="30" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Naslov i Podaci -->
                        <TextBlock x:Name="txtDetaljiNaslov" FontSize="26" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}" Margin="0,0,0,10" TextWrapping="Wrap"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="✍️ Autor: " Foreground="{DynamicResource PrimaryText}" FontSize="13"/>
                            <TextBlock x:Name="txtDetaljiAutor" Foreground="{DynamicResource PrimaryText}" FontSize="13" FontWeight="Bold" Margin="0,0,20,0"/>
                            <TextBlock Text="📅 Datum: " Foreground="{DynamicResource PrimaryText}" FontSize="13"/>
                            <TextBlock x:Name="txtDetaljiDatum" Foreground="{DynamicResource PrimaryText}" FontSize="13" FontWeight="Bold"/>
                        </StackPanel>

                        <!-- Sadržaj -->
                        <TextBlock x:Name="txtDetaljiSadrzaj" FontSize="15" Foreground="{DynamicResource PrimaryText}" LineHeight="24" TextWrapping="Wrap"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>