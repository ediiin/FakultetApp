<UserControl x:Class="FakultetApp.Views.Predmeti.PredmetPregledView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FakultetApp.Views.Predmeti"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#F5F5F5" Padding="10" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Pretraga po nazivu predmeta ili profesoru:" Margin="0,0,0,5" FontWeight="Bold"/>
                    <TextBox Name="txtFilter" Width="350" Height="25" TextChanged="Filter_TextChanged"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Name="dgvPredmeti" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naziv predmeta" Binding="{Binding Naziv}" Width="2*"/>
                <DataGridTextColumn Header="ECTS" Binding="{Binding ECTS}" Width="80"/>
                <DataGridTextColumn Header="Godina studija" Binding="{Binding GodinaStudija.Opis}" Width="1.5*"/>

                <!-- Kombinovani prikaz zvanja, imena i prezimena profesora unutar kolone -->
                <DataGridTemplateColumn Header="Profesor" Width="2.5*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,0,0">
                                <TextBlock Text="({Binding Profesor.ZvanjeOpis})" Margin="0,0,4,0" FontWeight="SemiBold" Foreground="#4A5568"/>
                                <TextBlock Text="{Binding Profesor.Ime}" Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding Profesor.Prezime}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Akcija" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="btnObrisi"
                                    Content="Ukloni predmet"
                                    Background="#FF4D4D"
                                    Foreground="White"
                                    FontWeight="Bold"
                                    Padding="5,2"
                                    Click="BtnObrisi_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
