<UserControl x:Class="FakultetApp.Views.Admin.AdminZahtjeviZaPotvrduView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <Grid Margin="10">
        <Border Background="{DynamicResource CardBackground}" 
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="1"
                CornerRadius="8" 
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" 
                           Text="Upravljanje zahtjevima za potvrdu" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource Naslov}" 
                           Margin="0,0,0,15"/>

                <ListView x:Name="lstSviZahtjevi" 
                          Grid.Row="1" 
                          Background="Transparent"
                          BorderThickness="0" 
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource CardBackground}" 
                                    BorderBrush="{DynamicResource BorderColor}" 
                                    BorderThickness="1" 
                                    CornerRadius="6" 
                                    Padding="15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="2.5*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Kolona 1: Student -->
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="Student:" FontSize="11" Foreground="{DynamicResource DisabledText}"/>
                                        <TextBlock Text="{Binding StudentInfo}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryText}"/>
                                    </StackPanel>

                                    <!-- Kolona 2: Svrha i Datum -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                                        <TextBlock Text="{Binding SvrhaText}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="{Binding DatumPodnosenjaText}" Foreground="{DynamicResource DisabledText}" FontSize="12"/>
                                    </StackPanel>

                                    <!-- Kolona 3: Napomena -->
                                    <TextBlock Grid.Column="2" Text="{Binding Napomena}" TextWrapping="Wrap" Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center" Margin="10,0"/>

                                    <!-- Kolona 4: Status Badge -->
                                    <Border Grid.Column="3" Background="{Binding StatusBojaPozadina}" CornerRadius="12" Padding="10,5" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusBojaTekst}" FontWeight="SemiBold"/>
                                    </Border>

                                    <!-- Kolona 5: Akcije (Odobri / Odbij) -->
                                    <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="{Binding PrikaziAkcije}">
                                        <Button Content="✔ Odobri" 
                                                Foreground="White" 
                                                Background="#28a745" 
                                                BorderThickness="0" 
                                                Padding="10,5"
                                                Margin="0,0,5,0"
                                                Cursor="Hand" 
                                                FontWeight="Bold"
                                                Click="BtnOdobri_Click" 
                                                Tag="{Binding Id}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Content="✖ Odbij" 
                                                Foreground="White" 
                                                Background="#dc3545" 
                                                BorderThickness="0" 
                                                Padding="10,5"
                                                Cursor="Hand" 
                                                FontWeight="Bold"
                                                Click="BtnOdbij_Click" 
                                                Tag="{Binding Id}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>